<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cskaoyan.erp.dao.SysUserDao">

    <sql id="sysuserColumns">
        id,username,password,locked
    </sql>

    <select id="findUserByUsername" resultType="sysuser">
      select <include refid="sysuserColumns"/>
      from sys_user where username = #{username}
    </select>

    <select id="findUserByUsernameAndPassword" resultType="sysuser">
        select <include refid="sysuserColumns"/>
        from sys_user
        where username = #{username} and password = #{password}
    </select>
</mapper>